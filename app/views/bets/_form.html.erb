<%= form_for(bet) do |f| %>
  <% if bet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bet.errors.count, "error") %> prohibited this bet from being saved:</h2>

      <ul>
      <% bet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :match %>
    <%= f.text_field :match %>
  </div>

  <div class="field">
    <%= f.label :home_win %>
    <%= f.text_field :home_win %>
  </div>

  <div class="field">
    <%= f.label :draw %>
    <%= f.text_field :draw %>
  </div>

  <div class="field">
    <%= f.label :away_win %>
    <%= f.text_field :away_win %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
